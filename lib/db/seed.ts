/**
 * Seed Service
 * Clean production mode: no demo users or mock database seeds are created.
 */
export async function ensureSeedData() {
  // Production clean mode: all user profiles & workspaces are created dynamically
  return;
}
